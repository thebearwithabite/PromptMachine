# Example environment variables for PromptMachine (client-side Vite app)
# Rename to .env.local and populate before running locally.

# Your GenAI / VEO API key (client-safe usage depends on your deployment strategy)
VITE_GENAI_API_KEY=your_genai_api_key_here

# Optional: preferred model to use (if the client allows switching models)
VITE_GENAI_MODEL=veo-3.1

# Optional: base URL for proxy or API server (if using a server-side proxy)
VITE_API_BASE_URL=https://api.example.com

# Optional: feature flags
VITE_ENABLE_LOGGING=true
